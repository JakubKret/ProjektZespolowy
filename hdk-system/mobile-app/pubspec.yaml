name: hdk_mobile_app
description: HDK system mobile app.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '^3.11.0'
  flutter: '>=3.35.0'

dependencies:
  flutter:
    sdk: flutter

  drift: ^2.20.0
  sqlite3_flutter_libs: ^0.5.0
  path_provider: ^2.1.0
  path: ^1.9.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

  drift_dev: ^2.20.0
  build_runner: ^2.4.0

flutter:
  uses-material-design: true
